#include <stdio.h>
#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 *@n: n is an integer
 *
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (0);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
